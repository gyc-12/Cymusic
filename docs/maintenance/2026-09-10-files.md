# 项目文件状态

清单生成时间：2026-09-10T07:25:35+08:00。分支 `main`，目标远端为
[`gyc-12/Cymusic`](https://github.com/gyc-12/Cymusic)。五个产品提交已完成，
产品源码提交为 `21e91d8d018bf7caba79ea499b6471644f41ae42`；本报告和清单随后作为文档提交。

**最终文档暂存范围内共有 304 个 Git 追踪路径。** 完整逐文件列表见
[2026-09-10-tracked-files.csv](2026-09-10-tracked-files.csv)，包含目录分类、Git 模式、
本地是否存在以及特殊处理说明。清单包含本文件和 CSV 自身；路径集合以本次提交索引
为准，不把未追踪文件算作已上传，也不使用会递归变化的文档自身对象哈希。

| 追踪范围 | 文件数 | 内容 |
| --- | ---: | --- |
| `src/` | 174 | 现有页面、路由、组件、播放/音源/数据 owner、工具与类型 |
| `ios/` | 29 | 保留的 iOS 工程、分享扩展、资源与 Pods 锁 |
| `modules/` | 18 | 自有 Expo Modules 与独立 JavaScriptCore 引擎 |
| `scripts/` | 17 | 可复现的专项回归与原生检查 fixture |
| `assets/` | 14 | 图片、默认资源和播放占位音频 |
| `patches/` | 4 | 四个精确版本的严格安装补丁 |
| `docs/` | 17 | 历史审计、当前依赖和文件清单、计划与结果 |
| `根目录` | 16 | 包声明、Yarn 锁、构建配置、README、许可证与保留压缩包 |
| `.github/` | 1 | 现有 iOS 构建工作流 |
| `.trellis/` | 1 | 精选共享原生契约；未纳入个人会话与任务原始证据 |
| `.claude/` | 5 | 原有音源插件开发知识 |
| `.idea/` | 7 | 仍在 Git 索引中的七个 IDE 配置；本地原有删除未暂存 |
| `.vscode/` | 1 | 现有编辑器配置 |
| **合计** | **304** | |

其中 **7 个路径在本地已删除，但删除尚未暂存**，均为用户原有 `.idea/` 改动。
它们仍会保留在此次推送的提交树中；本轮没有恢复、提交或覆盖这些删除。
根目录的 `src.zip` 也仍然追踪，作为历史材料保留。

生成清单时，本轮暂存的是 **20 个精选文档/忽略规则文件**，产品源码已经提交。
已纳入：应用与原生源码、锁文件、必要补丁、独立检查、12 个历史审计文件、
本轮计划/结果/依赖/文件清单，以及一份共享原生契约。

此时另有 **374 个未追踪且未被忽略的本地候选文件**，按顶层范围分组如下。
这些是本次快照，不承诺后续本地日志或任务归档后数量保持不变。

| 未追踪范围 | 文件数 |
| --- | ---: |
| `.agent/` | 46 |
| `.agents/` | 46 |
| `.claude/` | 52 |
| `.codex/` | 8 |
| `.factory/` | 53 |
| `.github/` | 53 |
| `.trellis/` | 114 |
| `根目录` | 2 |

这些候选包括个人平台配置、Trellis 运行器/任务/日志及可选团队配置；本轮只暂存了
上面明确列出的共享成果。Git 推送只发送已提交对象，未追踪文件不会自动上传。
原始研究下载、构建日志、截图及运行证据由 `/.trellis/tasks/**/research/` 忽略；
该规则也覆盖任务归档后的路径。废弃任务原始材料已移至项目外备份，未生成 Changelog。

最终依赖图为 **61 个第三方直接依赖（48 runtime / 13 development）**，逐项见
[依赖清单](2026-09-10-dependencies.csv)。自有本地 Expo 模块单独归在 `modules/`，
不计入第三方 npm 依赖数。版本、架构、替代决定与验证边界见
[结果报告](2026-09-10-results.md)；历史审计中的 251/389 等数字仅对应当时状态。
